apiVersion: v1
kind: ConfigMap
metadata:
  name: govuk-route-library-config
  labels:
    app: govuk-route-library
data:
  # Search API Configuration
  # Leave empty to use the built-in mock API at /api/search
  # Set to an external URL to use a different backend
  # Example: https://api.example.gov.uk
  # for demo: SEARCH_API_URL: "https://moj-search-app-moj-vector-data.apps.cluster-6ldk6.6ldk6.sandbox2187.opentlc.com/search"
  SEARCH_API_URL: "https://moj-search-app-moj-vector-data.apps.cluster-trdww.trdww.sandbox3406.opentlc.com"

  # Feature Flags
  # Set to 'true' to enable filters on the results page
  ENABLE_FILTERS: "false"
